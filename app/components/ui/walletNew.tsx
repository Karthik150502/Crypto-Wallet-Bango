import React from 'react'
import { Wallet } from '@/app/types/wallet'
export default function wWlletNew({ publicKey, privateKey, name, id }: Wallet) {
    return (
        <div className='flex items-center justify-between'>

        </div>
    )
}
